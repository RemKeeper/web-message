{
  "name": "web-message",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "go run github.com/syumai/workers/cmd/workers-assets-gen -mode=go && cross-env GOOS=js GOARCH=wasm go build -o ./build/app.wasm .",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "wrangler": "^4.9.0"
  }
}
